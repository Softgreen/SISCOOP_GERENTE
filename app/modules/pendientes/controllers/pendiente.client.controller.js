'use strict';

/* jshint -W098 */
angular.module('pendiente').controller('PendienteController', ['$scope', 'Auth',
    function ($scope) {

        $scope.package = {
            name: 'pendiente'
        };

    }
]);
